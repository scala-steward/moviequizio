# Movie Quiz

Built with [Scala.js](https://www.scala-js.org/) 

## Local development
- Compile and generate JavaScript (run inside `sbt`):
```
> fastLinkJS
```

- Open the game: open index.html in your favourite browser
